# =============================================================================
# Shagya — Environment Variables
# =============================================================================
# Copy to .env for local dev. Never commit .env.
# For CI/CD env vars, see docs/ci-cd.md
# =============================================================================

# --- Server ---
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# --- Database (Neon Postgres) ---
# Local: postgres://shagya:shagya_dev@localhost:5432/shagya
# CI/Prod: set via Vercel project env vars per environment
DATABASE_URL=postgres://shagya:shagya_dev@localhost:5432/shagya

# --- Payload ---
PAYLOAD_SECRET=change-me-use-openssl-rand-base64-32

# --- Cloudflare R2 / S3 ---
# Local: MinIO at localhost:9000 (see infra/dev-services.yml)
# CI/Prod: set via Vercel project env vars per environment
R2_ENDPOINT=http://localhost:9000
R2_ACCESS_KEY_ID=minioadmin
R2_SECRET_ACCESS_KEY=minioadmin
R2_BUCKET=shagya-dev
R2_REGION=us-east-1

# --- Resend (Transactional Email) ---
RESEND_API_KEY=re_xxxx

# --- Mailpit (Local SMTP — replaces Resend in dev) ---
# Started via `make infra-up`. Web UI: http://localhost:8025
MAILPIT_SMTP_HOST=localhost
MAILPIT_SMTP_PORT=1025
MAILPIT_API_URL=http://localhost:8025/api/v1

# --- Better Auth (Customer Auth) — Phase 2 ---
BETTER_AUTH_SECRET=change-me-use-openssl-rand-base64-32
BETTER_AUTH_URL=http://localhost:3000

# --- OAuth / Social Providers ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=
APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=

# --- Twilio (SMS / OTP) ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# --- Razorpay (Payments) — Phase 3 ---
RAZORPAY_KEY_ID=rzp_test_xxxx
RAZORPAY_KEY_SECRET=change-me

# --- Optional ---
# NEXT_PUBLIC_GA4_ID=G-XXXXXXXXXX
# SENTRY_DSN=https://xxxx.ingest.sentry.io/xxxx
